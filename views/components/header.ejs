<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-primary">
      <div class="container">
        <a class="navbar-brand text-light" href="/">Chat</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link text-light" href="#">Link</a>
            </li>
          </ul>

          <form class="d-flex w-100">
            <input class="form-control" type="search" placeholder="Search" aria-label="Search">
            <button class="btn btn-info" type="submit"><span class="material-icons">search</span></button>
          </form>
          <% if (user) { %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle text-light" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <%= user.firstName %>
              </a>
              <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                <li>
                  <a class="dropdown-item text-dark d-flex justify-content-between" href="/users/profile/<%= user._id %>">
                    <span>profile</span>
                    <span class="material-icons">account_box</span>
                  </a>
                </li>
                <li>
                  <a class="dropdown-item text-dark d-flex justify-content-between" href="/users/logout">
                    <span>logout</span>
                    <span class="material-icons">logout</span>
                  </a>
                </li>
                <% if (user.isAdmin == true) { %>
                  <li><hr class="dropdown-divider"></li>
                  <li>
                    <a class="dropdown-item text-dark d-flex justify-content-between" href="#">
                      <span>admin</span>
                      <span class="material-icons">lock</span>
                    </a>
                  </li>
                <% } %>
              </ul>
            </li>
          <% } else { %>
            <li class="nav-item ms-2">
              <a class="nav-link btn btn-light rounded-pill text-dark" href="/users/login">login</a>
            </li>
            <li class="nav-item ms-2">
              <a class="nav-link btn btn-light rounded-pill text-dark" href="/users/register">register</a>
            </li>
          <% } %>
        </div>
      </div>
    </nav>
